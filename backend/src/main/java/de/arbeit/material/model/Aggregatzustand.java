package de.arbeit.material.model;

/**
 * Der Aggregatzustand entscheidet im Frontend, welche Zusatzfelder
 * eingeblendet werden (z.B. Gasdruck nur bei GAS).
 */
public enum Aggregatzustand {
    FEST,
    GAS
}
